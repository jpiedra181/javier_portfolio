<!-- Skills -->
<section class="marquee whitespace-nowrap w-full overflow-hidden pt-30 sm:px-20 md:px-24 lg:px-30 2xl:px-40 flex flex-col items-center gap-10" id="skills">
    <h2 class="font-semibold text-4xl" data-en="Skills" data-es="Habilidades">Skills</h2>
    <div class="slider" style="
            --width: 100px;
            --height: 100px;
            --quantity: 7;
        ">
        <div class="list">
            <div class="item w-24" style="--position: 1"><img src="icons/html_icon.svg" alt=""></div>
            <div class="item w-24" style="--position: 2"><img src="icons/css_icon.svg" alt=""></div>
            <div class="item w-24" style="--position: 3"><img src="icons/tailwindcss_icon.svg" alt=""></div>
            <div class="item w-24" style="--position: 4"><img src="icons/javascript_icon.svg" alt=""></div>
            <div class="item w-24" style="--position: 5"><img src="icons/figma_icon.svg" alt=""></div>
            <div class="item w-24" style="--position: 6"><img src="icons/react_icon.svg" alt=""></div>
            <div class="item w-24" style="--position: 7"><img src="icons/typescript_icon.svg" alt=""></div>
        </div>
    </div>
</section>

<!-- Javascript file to change the language of the content -->
<script src="scripts/skillsLang.js" defer></script>

<style>
    .slider{
    width: 100%;
    height: var(--height);
    overflow: hidden;
    mask-image: linear-gradient(
        to right,
        transparent,
        #000 10% 90%,
        transparent
    );
    }
    .slider .list{
        display: flex;
        width: 100%;
        min-width: calc(var(--width) * var(--quantity));
        position: relative;
    }
    .slider .list .item{
        width: var(--width);
        height: var(--height);
        position: absolute;
        left: 100%;
        animation: autoRun 10s linear infinite;
        transition: filter 0.5s;
        animation-delay: calc( (10s / var(--quantity)) * (var(--position) - 1) - 10s)!important;
    }
    .slider .list .item img{
        width: 100%;
    }
    @keyframes autoRun{
        from{
            left: 100%;
        }to{
            left: calc(var(--width) * -1);
        }
    }
    .slider:hover .item{
        animation-play-state: paused!important;
        filter: grayscale(1);
    }
    .slider .item:hover{
        filter: grayscale(0);
    }
    .slider[reverse="true"] .item{
        animation: reversePlay 10s linear infinite;
    }
    @keyframes reversePlay{
        from{
            left: calc(var(--width) * -1);
        }to{
            left: 100%;
        }
    }
    
</style>